{{--
    503 - Under maintenance

    Rendered while the application is down for a deployment (`artisan down`) and
    whenever the platform is deliberately unavailable.

    The copy reassures rather than apologises in the abstract: it says the
    pause is short and that the participant's data is safe: a bare "server
    error" during an exam week is frightening and unhelpful (Article 15).

    Like the 500 page it resolves no user and queries nothing: during
    maintenance the database may legitimately be unreachable.

    @see PRD §11.1 · CONSTITUTION Articles 7, 15, 17
--}}

<x-layout.error-page code="503" />
