# دستور منصة أثر التدريبية — إصدار لارافيل

### الوثيقة الحاكمة لعمل الذكاء الاصطناعي والفريق البشري
**الإصدار:** 2.0 (لارافيل) · **التاريخ:** 7 سبتمبر 2026 · **الحالة:** سارٍ وملزم
**يَنسخ:** دستور الإصدار 1.0 (Next.js / Cloudflare) في كل ما يخص الستاك التقني فقط. المبادئ الحاكمة باقية كما هي.

---

> **إقرار تمهيدي — يجب قراءته بصدق**
>
> «نسبة خطأ صفر» لا تتحقق بالنوايا ولا بالتعليمات النثرية. تتحقق بأمرين فقط:
> **(١)** تقليص المساحة التي يُسمح فيها بالخطأ أصلًا، و**(٢)** جعل كل خطأ متبقٍّ **يُكتشف آليًا قبل أن يصل إلى الإنسان**.
>
> لذلك كل مادة في هذا الدستور إما **مفروضة بأداة آلية** — موسومة بـ 🔒 — وإما **ضبط بشري لا يمكن أتمتته** — موسومة بـ 👤. ما لا يمكن فرضه آليًا يُعلَن بوضوح ولا يُدَّعى.
>
> **التعهد المقدَّم:** لن يصل خطأ إلى الإنتاج دون أن يمرّ على بوابة صُمِّمت لاصطياد نوعه. وإن مرّ، **تُغلق الثغرة في البوابة قبل إصلاح الخطأ نفسه**.

---

## الباب الأول — المرجعية والسيادة

### المادة 1 · تسلسل المرجعية (عند التعارض، الأعلى يَحكُم)

| # | المرجع | الحُجّية |
|---|--------|----------|
| 1 | **قرار صاحب المنتج المكتوب** في `docs/03-decisions/DECISIONS.md` | نهائي ومطلق. يَنسخ ما دونه. |
| 2 | **هذا الدستور** | ملزم لكل عمل. لا يُخالَف إلا بقرار مكتوب من المستوى 1. |
| 3 | **عقد المشروع** `PROJECT-CONTRACT.md` | العقد التقني: الأسماء والتواقيع والمسارات. لا يُخالَف بلا تحديثه أولًا. |
| 4 | **وثيقة المتطلبات** `docs/00-source/PRD-AR-source.md` | المصدر الوظيفي الوحيد. |
| 5 | **وثائق التحليل** `docs/01-analysis/*` | ملزمة تقنيًا ما لم تُعارض 1–4، **وما لم تكن خاصة بستاك Cloudflare المهجور**. |
| 6 | **الأعراف الهندسية العامة** ولارافيل | تُتَّبع فقط حين تسكت 1–5. |

### المادة 2 · قاعدة السكوت 👤
سكوت المراجع الخمسة الأولى عن مسألة **ليس إذنًا بالاجتهاد**. هو **حالة تصعيد إلزامية** (المادة 31).

### المادة 3 · نطاق السريان
يسري على: كل سطر PHP و Blade و JS و CSS، كل ترحيل قاعدة بيانات، كل نص واجهة، كل أصل تصميمي، كل رسالة بريد، كل إعداد بنية تحتية، وكل commit — سواء أنتجه ذكاء اصطناعي أو إنسان.

### المادة 3 مكرر · ما نُسخ من الإصدار الأول
وثائق `docs/01-analysis/01-cloudflare-architecture.md` و`05-email-and-integrations.md` تصف بنية **مهجورة**. تُقرأ للاستئناس بالمتطلبات الوظيفية فقط (أنواع الرسائل، نصوصها العربية، التكاملات)، ويُهمَل كل ما فيها من: Workers · D1 · KV · R2 · Queues · Drizzle · Wrangler · OpenNext. البديل في `PROJECT-CONTRACT.md` §11.

---

## الباب الثاني — المبادئ الحاكمة

### المادة 4 · مبدأ عدم الافتراض *(المبدأ الأعلى)* 👤
**يُحظر تمامًا سدّ أي فجوة في المتطلبات بافتراض.**
عند الغموض: توقّف، سجّل السؤال في `DECISIONS.md` بحالة `مفتوح`، أكمل ما لا يعتمد عليه، وأبلغ. الافتراض الصامت هو **المخالفة الأشد**، لأنه الخطأ الوحيد الذي لا تكتشفه أي أداة.

**الاستثناء الوحيد:** إن كان التوقف يعطّل عملًا كثيرًا وكان للفجوة خيار افتراضي **آمن وقابل للتراجع**، يُنفَّذ بشرط تسجيله في `DECISIONS.md` بحالة `افتراض مؤقت — يحتاج اعتمادًا` **وإعلانه في التقرير**. الافتراض غير المعلَن ممنوع بإطلاق.

**ممنوع الافتراض إطلاقًا، بلا استثناء، في:** الحضور · الدرجات · الصلاحيات · الشهادات · وضع المعاينة.

### المادة 5 · مبدأ الخادم هو الحَكَم 🔒
كل قاعدة عمل، وكل فحص صلاحية، وكل نافذة زمنية، وكل قيد تحقق — **تُفرض في PHP على الخادم**.
حالة الواجهة انعكاسٌ للحقيقة لا مصدرٌ لها. **إخفاء زر ليس حماية. تعطيل حقل ليس تحققًا. `@can` في Blade ليس تفويضًا.**
لكل نقطة نهاية مغيِّرة للحالة: `FormRequest` + `Policy` + فحص نطاق في الاستعلام. الثلاثة معًا، لا اثنان.

### المادة 6 · مبدأ المصدر الواحد 🔒
لكل حقيقة في النظام مصدر واحد لا يُكرَّر:

| الحقيقة | المصدر الوحيد |
|---|---|
| اللون والمسافة والخط والظل | `resources/css/tokens.css` |
| كل نص عربي أو إنجليزي | `lang/ar/*.php` و `lang/en/*.php` |
| الوقت والمناطق الزمنية والنوافذ | `app/Services/Time/Clock.php` |
| فحص الصلاحيات | `app/Policies/*` و `app/Services/Permissions/*` |
| التحقق من المدخلات | `app/Http/Requests/*` |
| حساب الحضور | `app/Services/Attendance/AttendanceWindow.php` |
| حساب الدرجات | `app/Services/Grading/ScoreCalculator.php` |
| أهلية الشهادة | `app/Services/Certificates/CertificateEligibility.php` |
| اكتمال خطوات الرحلة | `app/Services/Journey/JourneyEvaluator.php` |
| المحتوى القابل للتغيير | قاعدة البيانات، يُدار من لوحة الإدارة (BR-31، BR-36) |

### المادة 7 · مبدأ الفشل الآمن 🔒
عند أي شك أو خطأ في منطق حرج: **ارفض العملية، لا تسمح بها**.
عند تعذّر تحديد الوقت → ارفض. عند تعذّر تحديد الصلاحية → `403`. عند تعذّر التحقق من ملكية المورد → `403`. لا «تمرير مؤقت».

### المادة 8 · مبدأ الأثر الكامل 🔒
كل عملية حساسة تُسجَّل في `audit_logs` **قبل** إتمامها لا بعدها، ومع: الفاعل، الفعل، الكيان، القيمة قبل، القيمة بعد، عنوان IP، ومعرّف المتصفح. `audit_logs` **لا يُعدَّل ولا يُحذف** — لا نموذج له `update()` ولا `delete()`، والصلاحية على مستوى مستخدم قاعدة البيانات لا تشمل `UPDATE` ولا `DELETE` على هذا الجدول.

---

## الباب الثالث — الستاك والبيئة

### المادة 9 · الستاك المعتمد 🔒

| الطبقة | التقنية | ملزم |
|---|---|---|
| إطار العمل | **Laravel 11.x** (PHP 8.2+) — ⚠️ **المستودع اليوم على `^12.61.1`؛ التعارض مسجَّل في `D-27` ولم يُعتمد بعد** | نعم |
| قاعدة البيانات | **MySQL 8.0** (أو MariaDB 10.6+) · محرك InnoDB · ترميز `utf8mb4_unicode_ci` | نعم |
| طبقة البيانات | **Eloquent** + ترحيلات في المستودع | نعم |
| القوالب | **Blade** + مكوّنات Blade | نعم |
| التنسيق | **Tailwind CSS** مع `tailwindcss-rtl` أو خصائص منطقية | نعم |
| التفاعل | **Alpine.js** — لا React ولا Vue | نعم |
| البناء | **Vite** | نعم |
| المصادقة | جلسات لارافيل على الخادم (`database` driver) | نعم |
| تجزئة كلمات المرور | **bcrypt** بكلفة **12** (المعيار: `argon2id` إن توفر على المضيف) | نعم |
| الاختبار | **Pest** (فوق PHPUnit) | نعم |
| التحليل الساكن | **Larastan** مستوى **8** | نعم |
| التنسيق | **Laravel Pint** | نعم |
| الاستضافة | **Hostinger — استضافة مشتركة** (cPanel، بلا صلاحيات جذر) | نعم |

### المادة 10 · قيود الاستضافة المشتركة — تُحترم في كل قرار 🔒
هذه ليست تفضيلات، هي **حدود بيئة الإنتاج**:

| القيد | الأثر الملزم |
|---|---|
| لا `supervisor` ولا عفاريت دائمة | الطابور `database` فقط، ويُصرَّف بـ `queue:work --stop-when-empty` من كرون. **يُمنع افتراض عامل دائم.** |
| كرون بدقة دقيقة واحدة | مهمة واحدة كل دقيقة تستدعي `schedule:run`. كل المهام المجدولة تمرّ منها. |
| لا Redis مضمون | التخزين المؤقت والجلسات على `database` أو `file`. **يُمنع افتراض Redis.** |
| لا صلاحية جذر | لا امتدادات PHP إضافية، لا `exec` على أدوات نظام، لا تعديل `php.ini` عالميًا. |
| جذر الويب `public_html` | التطبيق خارج جذر الويب. راجع `deploy/README.md`. |
| حدود ذاكرة ووقت تنفيذ | لا عملية تتجاوز 30 ثانية في طلب ويب. الثقيل يذهب للطابور. |
| لا `pcntl` ولا `posix` | لا اعتماد عليهما في أي كود. |

### المادة 11 · الوقت 🔒
- التخزين **UTC** دائمًا في قاعدة البيانات (`config/app.php` → `timezone => 'UTC'`).
- العرض **Asia/Riyadh** دائمًا.
- **وقت الخادم هو المرجع الوحيد** (BR-07). لا يُقبل أي وقت مرسل من العميل، ولا يُوثق بساعة المتصفح.
- **يُمنع منعًا باتًا** استدعاء `now()` أو `Carbon::now()` أو `new DateTime()` أو `time()` أو `date()` خارج `app/Services/Time/Clock.php`. المصدر الوحيد: `Clock::now()`.
- في الاختبارات: `Clock::fake($instant)` — لا `Carbon::setTestNow()` مباشرة.

### المادة 12 · البيئات

| البيئة | النطاق | البيانات |
|---|---|---|
| محلية | `localhost` | قاعدة محلية ببيانات بذر |
| اختبار | `staging.ai.wareed.vip` | نسخة مطابقة ببيانات وهمية |
| إنتاج | `ai.wareed.vip` | بيانات حقيقية · نسخ احتياطي يومي |

`APP_DEBUG=false` في الإنتاج — **بلا استثناء**. `APP_URL` مصدر كل رابط مطلق (BR-36).

> **تصحيحان مؤرَّخان — كلاهما تطبيقٌ للمادة 1 لا تعديلٌ للدستور بمعنى المادة 32،**
> لأن قرار المالك المكتوب **المستوى 1** وهذا الدستور **المستوى 2**:
>
> 1. **7 سبتمبر 2026 —** كانت المادة تقول `ai.athar-dev.edu.sa`، فصُحِّحت إلى
>    `ai101.athar-dev.edu.sa` بقرار المالك `C-02` (6 سبتمبر 2026). التفصيل في `D-24`.
> 2. **7 سبتمبر 2026، لاحقًا في اليوم —** انتقلت الاستضافة إلى حساب Hostinger على
>    `wareed.vip`، فصار النطاق `ai.wareed.vip` بقرار مالك مكتوب جديد يَنسخ سابقه.
>    التفصيل وقائمة الملفات في `D-36`.

---

## الباب الرابع — ممنوعات تُوقف الدمج 🔒

### المادة 13 · القائمة السوداء
كل بند هنا يُفحص آليًا في `composer run gates`. وجود أي منها **يمنع الدمج**:

| # | الممنوع | الأداة |
|---|---|---|
| 1 | `dd()` · `dump()` · `var_dump()` · `print_r()` · `ray()` · `console.log` | grep + Pint |
| 2 | `now()` · `Carbon::now()` · `new DateTime` · `time()` · `date(` خارج `Clock` | grep مخصص |
| 3 | نص عربي مكتوب داخل ملف `.php` أو `.blade.php` خارج `lang/` | grep على النطاق `\x{0600}-\x{06FF}` |
| 4 | قيمة لون أو مسافة أو خط مباشرة في Blade أو CSS خارج `tokens.css` | grep على `#[0-9a-fA-F]{3,8}` و `rgb(` |
| 5 | **أي أصفر أو ذهبي** بأي درجة | grep على قائمة أكواد + فحص القناة |
| 6 | استعلام Eloquent بلا تقييد نطاق في مسار محمي | مراجعة + اختبار تفويض |
| 7 | نقطة نهاية مغيِّرة للحالة بلا `FormRequest` وبلا `Policy` | اختبار تفويض إلزامي |
| 8 | `Model::unguard()` · `$guarded = []` · `forceFill` على مدخل مستخدم | grep |
| 9 | استعلام خام بمعامل غير مربوط (`DB::raw` بمتغير) | grep + Larastan |
| 10 | سرّ في المستودع (`.env`، مفتاح، كلمة مرور) | secretlint + `.gitignore` |
| 11 | حذف فعلي (`forceDelete`) لمستخدم أو حضور أو تقييم أو شهادة | grep + اختبار |
| 12 | `--no-verify` على أي commit | خطّاف Git |
| 13 | `@php` في Blade يحوي منطق عمل | grep |
| 14 | `env()` خارج `config/` | grep — لأن `config:cache` يُبطلها |
| 15 | ترحيل يحذف عمودًا فيه بيانات بلا نسخة احتياطية موثّقة | مراجعة بشرية 👤 |

### المادة 14 · الهوية — ثوابت لا تُناقَش 🔒

| | |
|---|---|
| البنفسجي | `#7845B5` — `--brand-primary` |
| التركوازي | `#01AAAD` — `--brand-secondary` |
| الأبيض / الأسود | `#FFFFFF` / `#000000` |
| **ممنوع** | **الذهبي والأصفر بكل درجاتهما** — لا في زر ولا شارة ولا رسم بياني ولا تنبيه ولا تدرّج ولا ظل |
| التحذير | `#C97A17` برتقالي محروق — بمساحة ضيقة جدًا فقط |
| الشعار | `brand/svg/` (wordmark) · `brand/icons/` (mark للأحجام الصغيرة) |
| الشعار الأصلي | `ATHAR.pdf` — مُستخرَج كـ SVG مطابق |
| الشعار النصي | «من هنا يبدأ الأثر» |

**قاعدة تباين ملزمة:** الأبيض فوق التركوازي `#01AAAD` **لا** يحقق 4.5:1 للنص الصغير. يُستخدم الأسود أو `#017E80`.

---

## الباب الخامس — العربية والاتجاه

### المادة 15 · قواعد اللغة العربية 🔒

| القاعدة | التفصيل |
|---|---|
| كل النصوص في `lang/` | لا نص عربي في مكوّن أو متحكّم أو نموذج |
| الأرقام لاتينية | `1234` في كل المنصة — لا أرقام هندية |
| التواريخ | «الأحد 12 أكتوبر 2026» |
| الأوقات | 12 ساعة بـ «صباحًا» / «مساءً» |
| رسائل الخطأ | عربية، تشرح **ما حدث وما الحل**، بلا لوم وبلا مصطلح تقني |
| الأزرار | أفعال واضحة: «سجّل الآن» · «احفظ التغييرات» · «سلّم المهمة» — لا «موافق» و«إلغاء» وحدهما |
| العدد | يُراعى المفرد والمثنّى وجمع القلّة والكثرة: «جلسة واحدة» · «جلستين» · «3 جلسات» · «11 جلسة» |

### المادة 16 · قواعد RTL 🔒
- **خصائص منطقية فقط**: `margin-inline-start` لا `margin-left` · `inset-inline-end` لا `right` · `padding-block` لا `padding-top/bottom`.
- الأيقونات الاتجاهية (الأسهم، الترتيب، السهم الخلفي) **تُعكس**.
- **أشرطة التقدم تمتلئ من اليمين** — يُفحص بصريًا في كل شريط.
- القائمة الجانبية على **اليمين**: 264px مفتوحة · 72px مطوية · Drawer على الجوال.
- الحقول اللاتينية (البريد، الروابط، GitHub) `dir="ltr"` داخل الحقل مع بقاء التسمية RTL.
- `<html lang="ar" dir="rtl">`.

### المادة 16 مكرر · قاعدة اتصال الحروف العربية 🔒 *(درس مستفاد من العمل الفعلي)*
**يُمنع منعًا باتًا تقسيم نص عربي إلى حروف داخل عناصر منفصلة** (`<span>` لكل حرف) لأي غرض — حركة، تلوين، تأثير.
الحروف العربية متصلة، والتقسيم يكسر شكل الكلمة فتظهر «ال ب رنامج» بدل «البرنامج».
**المسموح:** التقسيم على **مستوى الكلمة**، أو التأثير على العنصر كاملًا (قناع، `clip-path`، شفافية).

---

## الباب السادس — كل شاشة وكل مكوّن

### المادة 17 · الحالات الأربع الإلزامية 🔒
لكل شاشة ولكل مكوّن يعرض بيانات، أربع حالات — **بلا استثناء**:

| الحالة | الشرط |
|---|---|
| **عادي** | المحتوى الحقيقي من قاعدة البيانات |
| **تحميل** | `Skeleton` **بشكل المحتوى القادم** — لا دوّامة فارغة ولا شاشة بيضاء |
| **فارغ** | رسم + عنوان + شرح + زر إجراء، **بنص خاص بهذه الشاشة تحديدًا** |
| **خطأ** | رسالة عربية تشرح ما حدث وما الحل |

**اختبار القبول:** لكل شاشة اختبار يثبت وجود الحالات الأربع.

### المادة 18 · إمكانية الوصول — WCAG 2.1 AA 🔒

| المتطلب | الحد |
|---|---|
| تباين النص العادي | ≥ 4.5:1 |
| تباين النص الكبير والعناصر التفاعلية | ≥ 3:1 |
| مساحة اللمس | ≥ 44 × 44 بكسل |
| حلقة التركيز | مرئية على كل عنصر تفاعلي |
| التنقل بلوحة المفاتيح | كامل، بترتيب منطقي |
| حصر التركيز | داخل كل نافذة منبثقة، ويعود للمصدر عند الإغلاق |
| `prefers-reduced-motion` | **محترم بالكامل** — كل حركة تُلغى وتُستبدل بتغيّر فوري |
| التكبير | حتى 200% دون فقد محتوى |
| اللون وحده | **لا يُستخدم لنقل معنى** — يُرفق دائمًا بأيقونة أو نص |
| `aria-live` | لكل رسالة نجاح أو خطأ ديناميكية |

### المادة 19 · الأداء 🔒

| المؤشر | المستهدف |
|---|---|
| FCP | < 1.5 ثانية |
| LCP | < 2.5 ثانية |
| CLS | < 0.1 |
| زمن الاستعلام الشائع | < 300 مللي ثانية |
| Lighthouse على الجوال لصفحة الهبوط | ≥ 90 في كل المحاور |
| JS في صفحة الهبوط | ≤ 40 كيلوبايت مضغوطًا |
| **استعلامات N+1** | **صفر** — يُفرض بـ `Model::preventLazyLoading()` في غير الإنتاج |
| تقسيم الصفحات | إلزامي لأي قائمة > 50 عنصرًا |
| الخطوط | مستضافة محليًا مع `font-display: swap` |

---

## الباب السابع — المنطق الحرج

### المادة 20 · اختبار قبل تنفيذ — بلا استثناء 🔒
النطاقات التالية **يُكتب اختبارها قبل كودها**:
**نوافذ الحضور · حساب الدرجات · فحص الصلاحيات · وضع المعاينة · شروط الشهادة · اكتمال خطوات الرحلة.**

**اختبار الحدود عند ±1 ثانية من كل حد زمني — إلزامي.**
مثال لجلسة من `S` إلى `E`: يُختبر `S-30m-1s` و `S-30m` و `S-30m+1s` و `S+30m-1s` و `S+30m` و `S+30m+1s` و `E-1s` و `E` و `E+1s` و `E+30m` و `E+30m+1s`.

### المادة 21 · قواعد العمل الست والثلاثون 🔒
`BR-01` … `BR-36` في `docs/00-source/PRD-AR-source.md` §10 **ملزمة حرفيًا**.
**لكل قاعدة اختبار واحد على الأقل يحمل معرّفها في اسمه.**
```php
it('BR-03: تسجيل الحضور بعد S+30m يُحتسب متأخرًا', function () { … });
```
**قاعدة بلا اختبار = قاعدة غير منفَّذة.**

### المادة 22 · التفويض 🔒
- كل صلاحية تُفحص على الخادم **في كل طلب** (BR-28).
- **قوائم سماح لا قوائم منع.**
- كل استعلام يُقيَّد بنطاق المستخدم: المشارك يقرأ سجلاته فقط، والمدرب دفعاته فقط (BR-22، BR-23).
- منع الوصول الأفقي: التحقق أن المورد يخص الطالب فعلًا — **تغيير المعرّف في الرابط يجب أن يُرجع 403**.
- كل رفض يُسجَّل في `audit_logs` مع عنوان IP.
- الروابط المباشرة للملفات **لا تُكشف**؛ رابط موقّع مؤقت صالح **15 دقيقة** بعد فحص الصلاحية.
- يبقى مدير نظام فعّال **واحد على الأقل** دائمًا (BR-32)، ولا يحذف المدير حسابه.

### المادة 23 · وضع المعاينة — أقوى صلاحية في المنصة 🔒
- قراءة فقط **صارمة، مفروضة في طبقة الوصول للبيانات نفسها** — لا في الواجهة. أي عملية كتابة تُرفض حتى لو استُدعيت نقطة النهاية مباشرة (BR-33).
- **لا يُغيَّر أي أثر للمستخدم**: لا آخر دخول، ولا قراءة إشعارات، ولا قراءة رسائل، ولا عدّادات تحميل (BR-34).
- شريط تنبيه ثابت طوال المعاينة بنص صريح وزر إنهاء ظاهر دائمًا.
- مدة الجلسة **30 دقيقة** ثم تنتهي تلقائيًا.
- **لا يمكن معاينة حساب مدير نظام آخر** (BR-35).
- البداية والنهاية وكل تنقل بين التبويبات يُسجَّل في `audit_logs`.

### المادة 24 · الأمان 🔒

| البند | الإلزام |
|---|---|
| CSRF | على كل عملية مغيِّرة للحالة |
| كوكي الجلسة | `HttpOnly` · `Secure` · `SameSite=Lax` |
| تجديد معرّف الجلسة | عند كل تسجيل دخول |
| تغيير كلمة المرور | يُبطل **كل** الجلسات النشطة (BR-29) |
| رسائل الدخول والاستعادة | **لا تكشف وجود البريد من عدمه** (BR-30) |
| رؤوس الأمان | CSP · HSTS · X-Content-Type-Options · X-Frame-Options · Referrer-Policy |
| XSS | كل محتوى مستخدم يُنقّى قبل العرض · **يُمنع `{!! !!}` على مدخل مستخدم** |
| SQL | استعلامات مربوطة عبر Eloquent حصرًا |
| الملفات | فحص MIME من **المحتوى** لا الامتداد · تخزين **خارج** جذر الويب · أسماء عشوائية |
| حدود الطلبات | حسب PRD §12.4 — تُطبَّق بـ `RateLimiter` |

---

## الباب الثامن — التتبع

### المادة 25 · التتبع في نفس الـ commit 🔒
1. **تعليق رأس كل ملف ميزة** يذكر معرّفات المتطلبات:
   ```php
   /**
    * نافذة تسجيل الحضور والانصراف.
    * @see BR-01, BR-02, BR-03, BR-04, BR-05, BR-06, BR-07 · PRD §9.9
    */
   ```
2. **اسم الاختبار يحمل المعرّف**: `it('BR-03: …')`.
3. **رسالة الـ commit**: `feat(attendance): نافذة تسجيل الحضور [BR-01,BR-02,FR-ATT-07]`.
4. **`docs/02-plan/TRACEABILITY.md` تُحدَّث الآن، لا لاحقًا.**

**متطلب بلا اختبار = متطلب غير منفَّذ.**

---

## الباب التاسع — البوابات

### المادة 26 · بوابات ما قبل الدمج `G1–G12` 🔒
تُشغَّل كلها بـ `composer run gates`. **فشل أي بوابة يمنع الدمج.**

| # | البوابة | الأمر | المعيار |
|---|---|---|---|
| **G1** | التنسيق | `pint --test` | صفر انحراف |
| **G2** | التحليل الساكن | `phpstan analyse` | مستوى 8 · صفر خطأ |
| **G3** | القائمة السوداء | `php artisan gate:forbidden` | صفر مطابقة (المادة 13) |
| **G4** | حصر الوقت | `php artisan gate:clock` | صفر `now()` خارج `Clock` |
| **G5** | حصر النصوص | `php artisan gate:i18n` | صفر نص عربي خارج `lang/` |
| **G6** | حصر التصميم | `php artisan gate:tokens` | صفر لون مباشر · **صفر أصفر أو ذهبي** |
| **G7** | الاختبارات | `pest` | كلها تمرّ |
| **G8** | تغطية المنطق الحرج | `pest --coverage --min=100` على `app/Services` | 100% |
| **G9** | التفويض | `pest --group=authz` | كل مسار محمي له اختبار 403 |
| **G10** | قواعد العمل | `php artisan gate:br` | لكل `BR-01..36` اختبار واحد على الأقل |
| **G11** | الترحيلات | `migrate:fresh --seed` ثم `migrate:rollback` | تعمل في الاتجاهين |
| **G12** | التتبع | `php artisan gate:traceability` | لا معرّف بلا سطر في `TRACEABILITY.md` |

### المادة 27 · مراجعة بعقل منفصل 👤
بعد كل دفعة، وقبل الدمج:
- **`athar-logic-verifier`** — أي مساس بالوقت أو الحضور أو الدرجات أو الشهادات أو الرحلة.
- **`athar-security-reviewer`** — أي مساس بالمصادقة أو الصلاحيات أو الرفع أو المعاينة أو الصفحات العامة.
- **`athar-ui-reviewer`** — أي مكوّن أو صفحة أو نمط.

---

## الباب العاشر — سير العمل

### المادة 28 · قبل أي دفعة عمل 👤
1. اقرأ متطلبات النطاق **من المصدر**، لا من ذاكرة ولا من ملخص.
2. اكتب خطة مصغّرة: المعرّفات · الملفات · الترحيلات · **أسماء الاختبارات** · الأسئلة المفتوحة · **ما لن يُنفَّذ**.
3. تحقّق أن `DECISIONS.md` لا يحوي بندًا `مفتوح` يعطّل هذه الدفعة.

### المادة 29 · الترحيلات 🔒
- كل ترحيل له `down()` يعمل فعلًا.
- **يُمنع تعديل ترحيل مدموج**؛ يُنشأ ترحيل جديد.
- كل مفتاح خارجي له `onDelete` صريح.
- كل قيد في PRD §7.7 له فهرس أو قيد فريد مطابق.
- البذر `database/seeders` يبني دفعة كاملة قابلة للاختبار يدويًا.

### المادة 30 · في التقرير 👤
يُذكر دائمًا:
- ما أُنجز · ما لم يُنجز · **ما فشل بنصه الحرفي** · ما افتُرض · ما انحرف عن الوثيقة ولماذا.
- **«يجب أن يعمل» ليست إجابة.**
- **لا يُبلَّغ عن إنجاز إلا بعد تشغيل فعلي.** إن تعذّر التشغيل، يُقال ذلك صراحة ويُذكر السبب.

### المادة 31 · صيغة التصعيد 👤
عند الغموض، يُكتب في `DECISIONS.md`:
```
### D-XX · [عنوان السؤال]
**الحالة:** مفتوح
**النطاق المتأثر:** …
**السؤال:** …
**الخيارات:** أ) … ب) … ج) …
**التوصية:** … ولماذا
**ما عُطِّل بسببه:** …
**ما أُكمل رغمه:** …
```
ثم يُبلَّغ في التقرير بصيغة صريحة: «توقفت عن كذا بانتظار قرارك في D-XX».

---

## الباب الحادي عشر — أحكام ختامية

### المادة 32 · تعديل هذا الدستور 👤
لا يُعدَّل إلا بقرار مكتوب من صاحب المنتج في `DECISIONS.md`، ويُرفع رقم إصداره، ويُذكر ما نُسخ.

### المادة 33 · عند التعارض بين مادة وأداة 🔒
إن سمحت أداة بما تمنعه مادة، **فالعيب في الأداة**. تُصلَح الأداة أولًا، ثم يُصلَح الكود.

### المادة 34 · الأولوية عند ضيق الوقت 👤
عند التعارض بين السرعة وأي مادة في البابين السادس والسابع (الحالات الأربع، إمكانية الوصول، المنطق الحرج، التفويض) — **المادة تُقدَّم والسرعة تُؤجَّل**. يُبلَّغ صاحب المنتج بالتأخير ولا يُخفَّض المعيار.

---

**نهاية الدستور — الإصدار 2.0**
