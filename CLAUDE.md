# منصة أثر التدريبية — تعليمات العمل الدائمة (إصدار لارافيل)

> **اقرأ `CONSTITUTION.md` كاملًا قبل أي عمل، ثم `PROJECT-CONTRACT.md`.**
> هذا الملف **تشغيلي ومختصر**؛ الدستور هو الحاكم، والعقد هو مصدر الأسماء والتواقيع. عند التعارض بين هذا الملف وأيّهما، **هما يفوزان وهذا الملف يُصحَّح**.

## المشروع
منصة البرامج التدريبية لمركز أثر — أول برنامج: **البرنامج التأسيسي في الذكاء الاصطناعي (AI 101)**.
**Laravel 11 · PHP 8.2+ · MySQL 8 · Blade · Alpine.js · Vite · Pest**
الاستضافة: **Hostinger — استضافة مشتركة** (cPanel، بلا صلاحية جذر) · الاتجاه: **RTL عربي** · العرض: `Asia/Riyadh` · التخزين: `UTC`.
النطاق: ⚠️ **متعارض ولم يُحسم — راجع `D-24`** (`ai` مقابل `ai101`). لا تكتب نطاقًا في الكود؛ `config/athar.php` وحده (BR-36).

## الوثائق الحاكمة — بالترتيب
| # | الملف | الدور |
|---|-------|------|
| 1 | `docs/03-decisions/DECISIONS.md` | **الأعلى حُجّية.** يَنسخ ما يخالفه. |
| 2 | `CONSTITUTION.md` | قواعد العمل الملزمة والبوابات |
| 3 | `PROJECT-CONTRACT.md` | الأسماء · التواقيع · الجداول · المسارات |
| 4 | `docs/00-source/PRD-AR-source.md` | المصدر الوظيفي الوحيد |
| 5 | `docs/01-analysis/*` | ملزم تقنيًا **ما لم يكن خاصًّا بستاك Cloudflare المهجور** |
| — | `*-v1-cloudflare.md` | **سجل تاريخي. لا يُستشهد به.** أرقام قراراته ومسارات ملفاته لم تعد تعني ما كانت تعنيه. |

## قبل أي دفعة عمل
1. اقرأ متطلبات النطاق **من المصدر**، لا من ذاكرة ولا من ملخص.
2. اكتب خطة مصغّرة: المعرّفات · الملفات · الترحيلات · **أسماء الاختبارات** · الأسئلة المفتوحة · **ما لن يُنفَّذ**.
3. تحقّق أن `DECISIONS.md` لا يحوي بندًا `مفتوح` يعطّل هذه الدفعة (القسم «سابعًا» جدول مختصر).

**البنود المعطِّلة اليوم:** `D-02` البريد · `D-11` آلية التسجيل · `D-17` صيغ الملفات · `D-24` النطاق · `D-08` تحصين `audit_logs` · `D-25` كلمة مرور صندوق البريد.

## المنطق الحرج — اختبار قبل تنفيذ، بلا استثناء
**نوافذ الحضور · حساب الدرجات · فحص الصلاحيات · وضع المعاينة · شروط الشهادة · اكتمال خطوات الرحلة.**
اختبر الحدود عند **±1 ثانية** من كل حد زمني. لجلسة من `S` إلى `E`: `S-30m-1s` · `S-30m` · `S-30m+1s` · `S+30m-1s` · `S+30m` · `S+30m+1s` · `E-1s` · `E` · `E+1s` · `E+30m` · `E+30m+1s`.

## ممنوعات توقف الدمج (الدستور، المادة 13)
`dd()` · `dump()` · `var_dump()` · `print_r()` · `console.log` ·
`now()` · `Carbon::now()` · `new DateTime` · `time()` · `date(` خارج `app/Services/Time/Clock.php` ·
**نص عربي داخل `.php` أو `.blade.php`** خارج `lang/` ·
لون أو مسافة أو خط مباشر في Blade أو CSS خارج `tokens.css` ·
**أي أصفر أو ذهبي بأي درجة، في أي مكان** ·
استعلام Eloquent بلا تقييد نطاق في مسار محمي ·
نقطة نهاية مغيِّرة للحالة بلا `FormRequest` **و** `Policy` ·
`Model::unguard()` · `$guarded = []` · `forceFill` على مدخل مستخدم ·
`DB::raw` بمتغير غير مربوط ·
سرّ في المستودع ·
`forceDelete` لمستخدم أو حضور أو تقييم أو شهادة ·
`@php` في Blade يحوي منطق عمل ·
**`env()` خارج `config/`** (لأن `config:cache` يُبطلها) ·
`--no-verify` على أي commit.

## المصدر الواحد لكل شيء (المادة 6)
| الحقيقة | المصدر الوحيد |
|---|---|
| اللون والمسافة والخط والظل | `resources/css/tokens.css` |
| كل نص | `lang/ar/*.php` · `lang/en/*.php` |
| الوقت والنوافذ | `app/Services/Time/Clock.php` |
| فحص الصلاحيات | `app/Policies/*` · `app/Services/Permissions/*` |
| التحقق من المدخلات | `app/Http/Requests/*` |
| حساب الحضور | `app/Services/Attendance/AttendanceWindow.php` |
| حساب الدرجات | `app/Services/Grading/ScoreCalculator.php` |
| أهلية الشهادة | `app/Services/Certificates/CertificateEligibility.php` |
| خطوات الرحلة | `app/Services/Journey/JourneyEvaluator.php` |
| ثوابت المنصة | `config/athar.php` ← `.env` (BR-36) |
| المحتوى القابل للتغيير | قاعدة البيانات، يُدار من لوحة الإدارة (BR-31) |

## الخادم هو الحَكَم (المادة 5)
**إخفاء زر ليس حماية. تعطيل حقل ليس تحققًا. `@can` في Blade ليس تفويضًا.**
لكل نقطة نهاية مغيِّرة للحالة: `FormRequest` + `Policy` + **تقييد نطاق في الاستعلام**. الثلاثة معًا، لا اثنان.
تغيير المعرّف في الرابط **يجب أن يُرجع 403**، ويُسجَّل الرفض في `audit_logs` مع عنوان IP.

## الهوية — ثوابت
| | |
|---|---|
| البنفسجي | `#7845B5` — `--brand-primary` |
| التركوازي | `#01AAAD` — `--brand-secondary` |
| الأبيض / الأسود | `#FFFFFF` / `#000000` |
| **ممنوع** | **الذهبي والأصفر بكل درجاتهما** — لا زر ولا شارة ولا رسم بياني ولا تنبيه ولا تدرّج ولا ظل |
| التحذير | `#C97A17` برتقالي محروق — بمساحة ضيقة جدًا فقط |
| الشعار | `brand/svg/` (wordmark) · `brand/icons/` (mark للأحجام الصغيرة) |
| الشعار النصي | «من هنا يبدأ الأثر» |

**الأبيض فوق التركوازي `#01AAAD` نسبته 2.86 — يسقط في معيار 4.5:1.** استخدم الأسود أو `#017E80`.

## قاعدة اتصال الحروف العربية — تُنسى وتُفسد الواجهة
**يُمنع منعًا باتًا تقسيم نص عربي إلى حروف داخل عناصر منفصلة** (`<span>` لكل حرف) لأي غرض: حركة، تلوين، تأثير، عدّاد.
الحروف العربية متصلة، والتقسيم يكسر شكل الكلمة فتظهر «ال ب رنامج» بدل «البرنامج».
**المسموح:** التقسيم على **مستوى الكلمة**، أو التأثير على العنصر كاملًا (قناع، `clip-path`، شفافية، `transform`).

## قواعد ثابتة تُنسى كثيرًا
- **كل شاشة وكل مكوّن يعرض بيانات**: أربع حالات — عادي · تحميل (skeleton **بشكل المحتوى القادم**، لا دوّامة) · فارغ (رسم + عنوان + شرح + زر، **بنص خاص بهذه الشاشة**) · خطأ.
- **كل رسالة خطأ**: عربية، تشرح **ما حدث وما الحل**، بلا لوم وبلا مصطلح تقني.
- **RTL**: خصائص منطقية فقط (`margin-inline-start` لا `margin-left`) · الأيقونات الاتجاهية تُعكس · **أشرطة التقدم تمتلئ من اليمين** · القائمة الجانبية على **اليمين** (264px مفتوحة · 72px مطوية · Drawer على الجوال) · الحقول اللاتينية `dir="ltr"` داخل الحقل مع بقاء التسمية RTL · `<html lang="ar" dir="rtl">`.
- **الأرقام لاتينية** (1234) · التواريخ «الأحد 12 أكتوبر 2026» · الأوقات 12 ساعة بـ صباحًا/مساءً · العدد يراعي المفرد والمثنّى والجمع.
- **مساحة اللمس** ≥ 44×44px · **التباين** ≥ 4.5:1 (و3:1 للكبير والتفاعلي) · حلقة تركيز مرئية · `prefers-reduced-motion` **محترم بالكامل** · اللون وحده لا ينقل معنى · `aria-live` لكل رسالة ديناميكية.
- **الوقت**: تخزين UTC، عرض `Asia/Riyadh`، **وقت الخادم هو المرجع الوحيد** (BR-07). لا يُقبل وقت من العميل. في الاختبارات `Clock::fake($instant)` لا `Carbon::setTestNow()`.
- **الاستضافة المشتركة**: لا عفاريت · لا Redis · الطابور `database` بـ `queue:work --stop-when-empty` من كرون · لا عملية تتجاوز 30 ثانية في طلب ويب · لا `pcntl` ولا `posix`.
- **الأداء**: صفر استعلامات N+1 (`Model::preventLazyLoading()` في غير الإنتاج) · تقسيم صفحات إلزامي لأي قائمة > 50 عنصرًا.
- **الملفات**: فحص MIME من **المحتوى** لا الامتداد · تخزين **خارج** جذر الويب · أسماء عشوائية · رابط موقّع صالح **15 دقيقة** بعد فحص الصلاحية.

## التتبع — إلزامي في نفس الـ commit (المادة 25)
- **تعليق رأس كل ملف ميزة** يذكر معرّفات المتطلبات:
  ```php
  /**
   * نافذة تسجيل الحضور والانصراف.
   * @see BR-01, BR-02, BR-03, BR-04, BR-05, BR-06, BR-07 · PRD §9.9
   */
  ```
- **اسم الاختبار يحمل المعرّف**: `it('BR-03: تسجيل الحضور بعد S+30m يُحتسب متأخرًا', …)`
- **رسالة الـ commit**: `feat(attendance): نافذة تسجيل الحضور [BR-01,BR-02,FR-ATT-07]`
- **`docs/02-plan/TRACEABILITY.md` تُحدَّث الآن، لا لاحقًا.**
- **متطلب بلا اختبار = متطلب غير منفَّذ.**

## بعد كل دفعة — البوابات `G1`…`G12`
```sh
composer run gates
```
| # | البوابة | الأمر | المعيار |
|---|---|---|---|
| G1 | التنسيق | `vendor/bin/pint --test` | صفر انحراف |
| G2 | التحليل الساكن | `vendor/bin/phpstan analyse` | مستوى 8 · صفر خطأ |
| G3 | القائمة السوداء | `php artisan gate:forbidden` | صفر مطابقة |
| G4 | حصر الوقت | `php artisan gate:clock` | صفر `now()` خارج `Clock` |
| G5 | حصر النصوص | `php artisan gate:i18n` | صفر نص عربي خارج `lang/` |
| G6 | حصر التصميم | `php artisan gate:tokens` | صفر لون مباشر · صفر أصفر أو ذهبي |
| G7 | الاختبارات | `vendor/bin/pest` | كلها تمرّ |
| G8 | تغطية المنطق الحرج | `vendor/bin/pest --coverage --min=100` | 100% على `app/Services` |
| G9 | التفويض | `vendor/bin/pest --group=authz` | لكل مسار محمي اختبار 403 |
| G10 | قواعد العمل | `php artisan gate:br` | لكل `BR-01..36` اختبار |
| G11 | الترحيلات | `migrate:fresh --seed` ثم `migrate:rollback` | تعمل في الاتجاهين |
| G12 | التتبع | `php artisan gate:traceability` | لا معرّف بلا سطر في `TRACEABILITY.md` |

ثم **مراجعة بعقل منفصل** (المادة 27):
- `athar-logic-verifier` — أي مساس بالوقت أو الحضور أو الدرجات أو الشهادات أو الرحلة
- `athar-security-reviewer` — أي مساس بالمصادقة أو الصلاحيات أو الرفع أو المعاينة أو الصفحات العامة
- `athar-ui-reviewer` — أي مكوّن أو صفحة أو نمط

## الترحيلات (المادة 29)
كل ترحيل له `down()` **يعمل فعلًا** · **يُمنع تعديل ترحيل مدموج** — يُنشأ ترحيل جديد · كل مفتاح خارجي له `onDelete` صريح · كل قيد في PRD §7.7 له فهرس أو قيد فريد مطابق · البذر يبني دفعة كاملة قابلة للاختبار يدويًا.

## عند الغموض
**توقّف.** لا تسدّ الفجوة بافتراض — **الافتراض الصامت هو المخالفة الأشد** لأنه الخطأ الوحيد الذي لا تكتشفه أي أداة (المادة 4).
سجّل في `DECISIONS.md` بحالة `مفتوح` وبصيغة المادة 31:
```
### D-XX · [عنوان السؤال]
**الحالة:** مفتوح
**النطاق المتأثر:** …
**السؤال:** …
**الخيارات:** أ) … ب) … ج) …
**التوصية:** … ولماذا
**ما عُطِّل بسببه:** …
**ما أُكمل رغمه:** …
```
ثم أكمل ما لا يعتمد عليه، وأبلغ: «توقفت عن كذا بانتظار قرارك في D-XX».

**الاستثناء الوحيد:** فجوة لها خيار افتراضي **آمن وقابل للتراجع** ويعطّل التوقف عملًا كثيرًا — يُنفَّذ بشرط تسجيله بحالة `افتراض مؤقت — يحتاج اعتمادًا` **وإعلانه في التقرير**.
**ممنوع الافتراض إطلاقًا، بلا استثناء، في:** الحضور · الدرجات · الصلاحيات · الشهادات · وضع المعاينة.

## صيغة التقرير (المادة 30)
اذكر دائمًا، بهذا الترتيب:
1. **ما أُنجز** — بالملفات والمعرّفات.
2. **ما لم يُنجز** — ولماذا.
3. **ما فشل — بنصّه الحرفي.** لا تلخيص رسالة الخطأ.
4. **ما افتُرض** — وأين سُجِّل.
5. **ما انحرف عن الوثيقة** — ولماذا.

**«يجب أن يعمل» ليست إجابة.**
**لا يُبلَّغ عن إنجاز إلا بعد تشغيل فعلي.** إن تعذّر التشغيل، يُقال ذلك صراحةً ويُذكر السبب. ادّعاء نجاح بوابة لم تُشغَّل مخالفة أشد من فشلها.
